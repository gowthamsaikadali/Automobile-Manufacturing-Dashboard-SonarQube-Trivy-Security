apiVersion: v1
kind: Service
metadata:
  name: autoforge-app
  namespace: {{ .Release.Namespace }}
spec:
  selector:
    app: autoforge
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
  type: ClusterIP
